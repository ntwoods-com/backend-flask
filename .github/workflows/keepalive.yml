name: Keepalive Ping

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

concurrency:
  group: keepalive
  cancel-in-progress: false

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
<<<<<<< HEAD
    if: ${{ secrets.RENDER_HEALTH_URL != 'https://backend-flask-bxpj.onrender.com/health' }}
=======
    if: ${{ vars.RENDER_HEALTH_URL != 'https://backend-flask-bxpj.onrender.com/health' }}
>>>>>>> e3def3d417a84047548ae88a15221f72ff20c607
    steps:
      - name: Ping /health
        run: |
          set -euo pipefail
            URL="${{ vars.RENDER_HEALTH_URL }}"
            URL="$(printf '%s' "$URL" | tr -d '\r\n' | sed 's/^ *//; s/ *$//')"
            [ -n "$URL" ] || (echo "RENDER_HEALTH_URL is empty" && exit 1)
            curl -fsSL --max-time 10 --retry 2 --retry-all-errors "$URL" >/dev/null
